package com.example.rces.service.impl;

import com.example.rces.repository.DocumentFilesRepository;
import com.example.rces.service.DocumentFilesService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
@Transactional(transactionManager = "primaryTransactionManager")
public class DocumentFilesServiceImpl implements DocumentFilesService {

    private final DocumentFilesRepository repository;

    @Autowired
    public DocumentFilesServiceImpl(DocumentFilesRepository repository) {
        this.repository = repository;
    }

    

}
