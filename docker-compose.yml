version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: rces-mysql
    environment:
      MYSQL_ROOT_PASSWORD: adminbms
      MYSQL_DATABASE: rces
    ports:
      - "3307:3306"
    volumes:
      - mysql:/var/lib/mysql

volumes:
  mysql: